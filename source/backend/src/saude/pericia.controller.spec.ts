import { PericiaController } from './pericia.controller';

describe('PericiaController', () => {
  it('is defined', () => {
    const controller = new PericiaController({} as never, {} as never);
    expect(controller).toBeDefined();
  });
});
