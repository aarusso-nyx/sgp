import { AuditSearchController } from './audit-search.controller';

describe('AuditSearchController', () => {
  it('is defined', () => {
    const controller = new AuditSearchController({} as never);
    expect(controller).toBeDefined();
  });
});
