AWSTemplateFormatVersion: '2010-09-09'
Description: SGP umbrella stack placeholder for Cognito, RDS, backend, and frontend.

Parameters:
  Environment:
    Type: String
    AllowedValues:
      - stage
      - prod
    Default: stage

Resources: {}

Outputs:
  NextStep:
    Description: Placeholder output to signal template completion requirements.
    Value: Replace this template with nested stack orchestration when infra inputs are finalized.
