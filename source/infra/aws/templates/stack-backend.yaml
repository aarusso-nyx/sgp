AWSTemplateFormatVersion: '2010-09-09'
Description: SGP backend container stack placeholder.

Parameters:
  Environment:
    Type: String
    AllowedValues:
      - stage
      - prod
    Default: stage
  BackendImageUri:
    Type: String
    Description: TODO_SET_BACKEND_IMAGE_URI

Resources: {}

Outputs:
  NextStep:
    Description: Placeholder output to signal template completion requirements.
    Value: Replace with ECS/Fargate (or equivalent), load balancer, and service autoscaling resources.
