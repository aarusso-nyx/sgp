{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Menus Inventory",
  "type": "object",
  "required": ["meta", "menus"],
  "properties": {
    "meta": {
      "type": "object",
      "required": ["version", "updatedAt", "source"],
      "properties": {
        "version": { "type": "string" },
        "updatedAt": { "type": "string" },
        "source": { "type": "string" }
      }
    },
    "menus": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["label", "menuPath", "status", "evidence"],
        "properties": {
          "id": { "type": "string" },
          "label": { "type": "string" },
          "menuPath": { "type": "array", "items": { "type": "string" } },
          "targetRoute": { "type": "string" },
          "module": { "type": "string" },
          "status": { "enum": ["observed", "inferred", "unverified"] },
          "evidence": { "type": "array", "items": { "type": "string" } }
        }
      }
    }
  }
}
