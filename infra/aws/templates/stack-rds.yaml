AWSTemplateFormatVersion: '2010-09-09'
Description: SGP RDS PostgreSQL stack placeholder.

Parameters:
  Environment:
    Type: String
    AllowedValues:
      - stage
      - prod
    Default: stage
  DbName:
    Type: String
    Default: sgp
  DbInstanceClass:
    Type: String
    Default: db.t4g.micro

Resources: {}

Outputs:
  NextStep:
    Description: Placeholder output to signal template completion requirements.
    Value: Replace with RDS subnet group, instance or cluster, security groups, and secrets integration.
