AWSTemplateFormatVersion: '2010-09-09'
Description: SGP frontend static hosting stack placeholder.

Parameters:
  Environment:
    Type: String
    AllowedValues:
      - stage
      - prod
    Default: stage
  FrontendDomainName:
    Type: String
    Description: TODO_SET_FRONTEND_DOMAIN

Resources: {}

Outputs:
  NextStep:
    Description: Placeholder output to signal template completion requirements.
    Value: Replace with S3 hosting, CloudFront distribution, certificate wiring, and DNS records.
