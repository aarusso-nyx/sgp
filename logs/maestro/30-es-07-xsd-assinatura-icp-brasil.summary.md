Executed ES-07 end-to-end by adding the committed official eSocial S-1.3 XSD bundle and manifest under `source/backend/src/esocial-worker/xsd/`, the libxmljs2 validator, XML-DSig ICP-Brasil signer, encrypted tenant certificate store with rotation APIs and RLS/audit migration, the `ESocialEmitService` gate that validates before queue insertion and signs before persisting to `public.esocial_event`, the admin certificate frontend under `source/frontend/src/app/features/esocial/certificados/`, focused unit/e2e/RLS tests, and docs in `docs/eng/40-divisao-modular.md`, `docs/eng/85-esocial-signature-hub.md`, and `docs/legacy-reverse/deprecation-status.md`; acceptance gates passed with `npm run lint`, `npm run typecheck`, `npm run test`, `DATABASE_URL=postgresql://aarusso@localhost:5432/pecam npm run test:e2e`, and `DATABASE_URL=postgresql://aarusso@localhost:5432/pecam npm run db:smoke`.
