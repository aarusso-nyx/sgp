CALC-01 completed after manual recovery from the exhausted retry: the canonical formula-attribute seed now types enum values through a text CTE, `payroll_calc` support SQL restores the compile trigger before cache materialization, and the TypeScript compiler emits two-argument `base_salary(employee, competence)` calls to avoid the salary-history overload while keeping compiled formula metadata in `payroll_calc.formula_cache`; final gates passed from `source` with `DATABASE_URL=postgresql://aarusso@localhost:5432/pecam`: `npm run lint`, `npm run typecheck`, `npm run test`, `npm run test:e2e`, `npm run db:smoke`, the five golden formula e2e scenarios passed, formula edits invalidate prior cache versions with `payroll.formula.cache_invalidated`, and direct `pg_policies` inspection confirmed `calc01_formula_cache_select` plus `calc01_formula_cache_write`.
