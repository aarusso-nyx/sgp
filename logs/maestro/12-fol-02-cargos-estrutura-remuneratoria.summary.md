FOL-02 was recovered after the retry failure by aligning `gestao.cargo.read` and `gestao.cargo.write` across the generated TypeScript permission catalog, canonical JSON seed, profile grants, and migration upserts against the current `public.permission` schema. The slice now delivers job position and salary range services/controllers, admin cargo UI, portal cargo card support, RLS policies for `hr.job_position`, `hr.salary_range`, and `hr.salary_range_level`, and a FOL-02 smoke block covering salary matrix non-null fields, `gestao.cargo.created` audit metadata, and cross-tenant RLS. Gates passed with `DATABASE_URL=postgresql://aarusso@localhost:5432/pecam npm run lint`, `npm run typecheck`, `npm run test`, `npm run test:e2e`, and `npm run db:smoke`.
