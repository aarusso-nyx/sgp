Implemented CALC-11 monthly payroll end-to-end orchestration with competence lifecycle, validation, paystub portal projection, admin and portal frontend surfaces, and focused monthly payroll/negative-net/portal visibility coverage. Manual recovery fixed the frontend query typing blocker, aligned new specs with Vitest, recompiled the monthly base salary rubric through the formula engine, added controller audit coverage for monthly mutations, and verified `lint`, `typecheck`, `test`, `test:e2e`, and `db:smoke` against `postgresql://aarusso@localhost:5432/pecam`.
