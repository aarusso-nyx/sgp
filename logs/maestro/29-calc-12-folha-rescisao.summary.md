Implemented CALC-12 termination payroll with the `compute_rescisao` database function, backend execution service and endpoint, admin/portal surfaces, portal career projection, docs updates, golden e2e scenarios, and an RLS coverage placeholder. The xhigh retry repaired the migration tenant context for formula-cache invalidation; manual recovery aligned the payroll controller unit fixture with the new `RescisaoService` dependency and made the CALC-12 e2e cleanup compatible with immutable history/audit tables. Verified `lint`, `typecheck`, `test`, `test:e2e`, `db:smoke`, and the `Math.round` regression scan against `postgresql://aarusso@localhost:5432/pecam`.
