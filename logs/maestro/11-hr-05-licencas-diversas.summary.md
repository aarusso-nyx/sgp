HR-05 Licencas Diversas foi implementado end-to-end: o backend agora expoe `POST /v1/licencas`, listagem por servidor, aprovacao e cancelamento em `source/backend/src/rh/workflows/leaves/`, com permissoes `rh.leave.request`, `rh.leave.read` e `rh.leave.approve`; o banco recebeu `paid`, dados de aprovacao/comprovante em `hr.leave_record`, validacao `hr.f_validate_leave_eligibility`, regras de duracao/quinquenio/comprovante, auditoria via `sgp_append_audit_event(...)`, RLS alinhado e smoke de tenant cruzado; o portal e a administracao receberam telas funcionais em `source/frontend/portal/src/app/pages/licencas/` e `source/frontend/src/app/features/rh/licencas/`; os documentos de engenharia foram atualizados para divisao modular, estados, manual do usuario e parametrizacao Empresa Cidada; a execucao final com `DATABASE_URL=postgresql://aarusso@localhost:5432/pecam` passou em `npm run lint`, `npm run typecheck`, `npm run test`, `npm run test:e2e` e `npm run db:smoke`, incluindo os criterios de maternidade 120/180 dias, capacitacao com menos de cinco anos rejeitada, interesse particular `paid=false`, aprovacao com `leave_record` + `audit_event` + `employee_status_history` e SELECT cross-tenant em `hr.leave_record` retornando zero linhas.
