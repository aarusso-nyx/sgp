FOL-03 foi implementado sobre os contratos atuais de FOL-02/FOL-04/FOL-05: a migração `20260501200000_fol03_progressao` adiciona nível salarial vigente ao servidor, tipo/status de progressão, vínculo de simulação e trigger de aplicação, com migrações complementares para registrar `salary_range_level_id` no histórico salarial aplicado e permitir que permissões de progressão leiam avaliações aprovadas em `hr.performance_evaluation`; o backend recebeu `source/backend/src/avaliacao/progression/` com elegibilidade por interstício e avaliação aprovada, simulação usando `avaliacao.fn_get_vencimento_vigente(...)` e `payroll_calc.evaluate_earning_deduction(...)`, aplicação auditada com `avaliacao.progressao.applied` e conflito 409 em reaplicação; a UI admin ganhou tela de progressões e o portal mostra a próxima progressão; permissões/RLS, matriz de features, máquina de estados e testes FOL-03 foram atualizados. Gates executados com sucesso: `npm run lint`, `npm run typecheck`, `npm run test`, `DATABASE_URL=postgresql://aarusso@localhost:5432/pecam npm run test:e2e`, `DATABASE_URL=postgresql://aarusso@localhost:5432/pecam npm run db:smoke`, `node --check scripts/db-bootstrap-smoke.mjs` e verificação direta de `pg_policies` para `hr.performance_evaluation`.
