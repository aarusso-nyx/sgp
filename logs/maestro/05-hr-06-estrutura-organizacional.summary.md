HR-06 closed organizational master data by adding hardened PostgreSQL persistence, permissions, RLS/audit triggers, e2e and RLS coverage, and the real Gestao master-data frontend routes/components for cargos, funções, lotações, centros de custo, and structure links. The retry failure in `db:smoke` was resolved with a corrective audit-trigger migration that falls back to `NEW.tenant_id` for seed/bootstrap inserts without overriding request tenant context; `lint`, `typecheck`, `test`, `test:e2e`, and `db:smoke` now pass against `postgresql://aarusso@localhost:5432/pecam`.
