CALC-02 completed after the high retry and maestro smoke recovery: the slice adds tenant-scoped IRRF tax-rate schema fields, seed data, RLS policies using `system.tax-rate.*`, admin CRUD/UI routing for tax rates, `payroll_calc.compute_irrf`, and an IRRF rubrica wired through the formula engine; the original `tax_rate` RLS smoke violation was corrected in support SQL, and the follow-on smoke uniqueness issue was fixed by separating the RLS smoke row from seeded IRRF bracket rows. Final gates passed from `source` with `DATABASE_URL=postgresql://aarusso@localhost:5432/pecam`: `npm run lint`, `npm run typecheck`, `npm run test`, `npm run test:e2e`, `npm run db:smoke`, CALC-02 golden IRRF e2e scenarios, and direct `pg_policies` inspection confirmed `tax_rate_select` plus `tax_rate_write`.
