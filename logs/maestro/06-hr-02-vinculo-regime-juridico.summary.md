HR-02 implemented regime-aware employment-link changes with database constraints for temporary, commissioned, and statutory regimes; a transactional `changeContractRegime` backend path with permission/audit coverage; generated API/frontend workflow support and a `funcionarios/vinculos` UI; focused unit, e2e, RLS, and smoke coverage; and docs updates for modular ownership, state transitions, and user guidance. Recovery aligned the migration with the current `public.access_profile`/`public.profile_permission` schema and updated smoke setup to use the existing master-data RLS permission model. Acceptance gates passed with `DATABASE_URL=postgresql://aarusso@localhost:5432/pecam`: `npm run lint`, `npm run typecheck`, `npm run test`, `npm run test:e2e`, and `npm run db:smoke`.
