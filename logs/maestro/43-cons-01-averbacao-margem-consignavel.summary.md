CONS-01 foi executado end-to-end na branch `slice/43-cons-01-averbacao-margem-consignavel`: a migration `20260502090000_cons_01_averbacao_margem` criou o schema `payment` com consignantes, averbações, materialized view de margem, RLS por `payment.consignment.read|write`, auditoria via `sgp_append_audit_event(...)` e permissões; o backend ganhou serviços/controllers para cálculo de margem geral/cartão, recusa HTTP 422 quando a averbação excede margem, emissão de descontos consignados na cadeia CALC-11 via `payroll_calc.evaluate_earning_deduction(...)`, testes unit/e2e e spec RLS; o frontend ganhou telas admin e portal de consignados, e os docs foram atualizados em `docs/eng/40-divisao-modular.md` e `docs/eng/90-margem-consignavel.md`; os gates `npm run lint`, `npm run typecheck`, `npm run test`, `npm run test:e2e` e `npm run db:smoke` passaram com `DATABASE_URL=postgresql://aarusso@localhost:5432/pecam`, com verificação adicional de que `payment.consignment_entity`, `payment.consignment_loan`, `payment.consignment_margin_view`, as policies RLS e as permissões existem no banco.
