ES-08 completed after manual recovery of the zero-retry lint blocker: the slice now replaces the sandbox dispatcher with SOAP submission batching, WS-Security envelope signing/verification, mTLS agent configuration, retry strategy, endpoint circuit breaker, submission admin UI, committed WSDL fixture, RLS smoke coverage, and `docs/eng/87-esocial-submission.md`. The recovery kept the third-party `soap` API behind typed `unknown` boundaries and normalized non-Error thrown values without disabling lint. Gates passed with `DATABASE_URL=postgresql://aarusso@localhost:5432/pecam`: `npm run lint`, `npm run typecheck`, `npm run test`, `npm run test:e2e`, `npm run db:smoke`, plus direct `pg_policies` verification for `esocial.submission_batch` and `esocial.endpoint_circuit_state`.
