XCUT-08 closed the money decimal policy by adding the engineering policy document, Decimal-based money helpers and tests, a custom ESLint guard against `Math.round` in monetary modules, and refactoring payroll amount calculations onto the new helpers. The decimal sweep migration was corrected to use the canonical `hr.pension_grant` table instead of the nonexistent `previdenciario` schema; the failed local Prisma attempt was marked rolled back and the corrected migration then applied successfully against `postgresql://aarusso@localhost:5432/pecam`.
