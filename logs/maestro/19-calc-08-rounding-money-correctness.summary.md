CALC-08 completed without retry: the existing `source/backend/src/common/money/money.ts` helper now exposes explicit `half_up` and `half_even` rounding modes, payroll advance serialization uses `roundMoney` instead of `Number(...).toFixed`, the local money lint rule also rejects `Number(...).toFixed(...)` in payroll calculation modules, `payroll_calc.evaluate_earning_deduction` rounds at the SQL boundary, the rubrica preview UI formats decimal strings through a `moneyBr` pipe, and the rounding policy doc includes the rubrica-to-mode matrix; final gates passed from `source` with `DATABASE_URL=postgresql://aarusso@localhost:5432/pecam`: `npm run lint`, `npm run typecheck`, `npm run test`, `npm run test:e2e`, `npm run db:smoke`, the target `Math.round` grep returned no matches, and the target `Number(...).toFixed` grep returned no matches.
