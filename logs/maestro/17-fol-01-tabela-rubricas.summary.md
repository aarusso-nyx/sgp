FOL-01 was completed in place and the retry repaired the failing db-smoke fixture by making the FOL-01 `payroll.formula_attribute` smoke `code`/`name` unique per run, avoiding the tenant-wide `formula_attribute_tenant_code_key` collision while preserving the existing rubrica implementation; final gates passed from `source` with `DATABASE_URL=postgresql://aarusso@localhost:5432/pecam`: `npm run lint`, `npm run typecheck`, `npm run test`, `npm run test:e2e`, `npm run db:smoke`, the slice readiness query returned `0` rows for `formula_expression IS NOT NULL AND formula_ready IS NULL`, the rubrica e2e contract asserts a numeric preview amount, and `public.audit_event` contains `folha.rubrica.created` events after creation.
