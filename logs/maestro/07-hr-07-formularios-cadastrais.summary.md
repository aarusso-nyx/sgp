HR-07 foi executado end-to-end: a fatia adicionou a tabela tenant-scoped `hr.cadastral_change_request` com workflow `PENDING/APPROVED/REJECTED`, auditoria por `sgp_append_audit_event(...)`, RLS estrita em `hr.employee_dependent` com `rh.dependent.*`, endpoints reais em `/v1/portal/meus-dados/*` e fila administrativa `/v1/funcionarios/cadastral-changes`, upload documental com checksum em `document_attachment`, telas Angular para portal e administração, permissões canônicas e documentação em `docs/eng`; os gates passaram com `DATABASE_URL=postgresql://aarusso@localhost:5432/pecam`: `npm run lint`, `npm run typecheck`, `npm run test`, `npm run test:e2e`, `npm run db:smoke`, e a checagem direta de `pg_policies` confirmou que `hr.employee_dependent` usa `sgp_tenant_matches(tenant_id) AND sgp_has_any_permission(ARRAY['rh.dependent.read','rh.dependent.write'])` sem bypass.
