HR-01 closed the employee admission and termination lifecycle by adding the admission/termination DTOs, service transactions, controller permissions, employment contract schema, immutable employee status timeline trigger, RLS/audit coverage, e2e tests, and a real `/rh/funcionarios` frontend page. Recovery normalized the permission catalog so broad `rh.read/write` remain workflow permissions while `rh.employee.*` owns employee lifecycle access, marked the failed Prisma attempt rolled back, and added an audit append security-definer correction plus smoke coverage; `lint`, `typecheck`, `test`, `test:e2e`, and `db:smoke` now pass against `postgresql://aarusso@localhost:5432/pecam`.
