HR-04 was recovered after the retry smoke failure by aligning the medical leave RLS model with the current schema: `medical_appointment`, `medical_record`, and `medical_leave` now keep granular HR-04 policies in both Prisma migrations and the shared bootstrap RLS script, the smoke fixture uses newline-delimited permission context with the required health/RH grants, and profile permission seeding targets the current `ADMIN` and `RH_OPERADOR` access profiles. Gates passed with `npm run lint`, `npm run typecheck`, `npm test`, `DATABASE_URL=postgresql://aarusso@localhost:5432/pecam npm run test:e2e`, `DATABASE_URL=postgresql://aarusso@localhost:5432/pecam npm run db:smoke`, and direct `pg_policies` inspection.
