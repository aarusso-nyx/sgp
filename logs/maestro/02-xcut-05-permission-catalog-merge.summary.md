Fixed the XCUT-05 retry failure by making `source/backend/test/app.e2e-spec.ts`'s `FakeDatabaseService` return catalog-backed `public.access_profile` / `profile_permission` / `permission` rows for the same SQL shape used by `PermissionsService`, preserving the global `PermissionGuard` default-deny behavior while allowing the existing e2e actors to resolve their intended permissions. Verified from `source/` with `npm run lint`, `npm run typecheck`, `npm run test`, `DATABASE_URL=postgresql://aarusso@localhost:5432/pecam npm run db:smoke`, `npm run prebuild` with unchanged generated catalog hashes, `npm --workspace backend run test -- catalog-drift.spec.ts`, and `DATABASE_URL=postgresql://aarusso@localhost:5432/pecam npm --workspace backend run test:e2e`.
