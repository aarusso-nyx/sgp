Implemented CLT-03 by adding the tenant-scoped `payment.pis_pasep_base_year` annual materialization, recompute function, RLS/audit coverage, PIS/PASEP permission catalog entries, backend service/controller, S-1200 and S-3000 recompute hooks, S-1010 PIS/PASEP incidence emission, an admin UI under folha, RLS/e2e/unit coverage, and `docs/eng/76-pis-pasep.md`; acceptance gates passed with `npm run lint`, `npm run typecheck`, `npm run test`, `DATABASE_URL=postgresql://aarusso@localhost:5432/pecam npm run test:e2e`, and `DATABASE_URL=postgresql://aarusso@localhost:5432/pecam npm run db:smoke`.
