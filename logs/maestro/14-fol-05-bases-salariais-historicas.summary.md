FOL-05 completed after high-tier recovery: the salary history migration and `source/database/sql/26-salary-history.sql` now provide vigencia-based salary lookup, overlap protection, audit logging, and RLS coverage; the migration no longer depends on an unset tenant context during seed insertion; the mocked transaction callback in `salary-history.service.spec.ts` is typed with `PoolClient` to satisfy lint; and the db smoke permission context now includes the FOL-02 cargo and auditoria permissions needed by the salary-history scenario. Verified with `npm run lint`, `npm run typecheck`, `npm run test`, `npm run test:e2e`, `npm run db:smoke`, and direct SQL checks for `avaliacao.fn_get_vencimento_vigente(...)` plus overlap rejection.
