BANK-04 foi concluído end-to-end: a tabela canônica `hr.employee_alimony` foi estendida in-place com dados de ordem judicial, beneficiário, conta judicial, base de cálculo, vigência, prioridade e status próprio, com `hr.employee_alimony_history`, RLS por `hr.alimony.read`/`hr.alimony.write`, trigger de `sgp_append_audit_event(...)` e permissão no catálogo; o backend ganhou CRUD `/api/v1/employees/:id/alimonies`, retenção prioritária em folha via `AlimonyDeductionService` chamando `payroll_calc.evaluate_earning_deduction(...)`, e a remessa CNAB 240 agora inclui repasses de pensão com `purpose_code` alimentício e vínculo `alimony_id` em `payroll.payment_remittance_detail`; o admin RH recebeu a tela de pensão alimentícia, e os docs foram atualizados em `docs/eng/40-divisao-modular.md`, `docs/eng/89-pensao-alimenticia.md` e `docs/legacy-reverse/deprecation-status.md`. Acceptance gates passaram com `DATABASE_URL=postgresql://aarusso@localhost:5432/pecam`: `npm run lint`, `npm run typecheck`, `npm run test`, `npm run test:e2e` e `npm run db:smoke`.
