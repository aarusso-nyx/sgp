FOL-06 completed after medium-tier recovery: the employee-transfer workflow now has schema support, RLS, permission catalog entries, backend services/controllers, admin and portal screens, and executable E2E/smoke coverage for request, approval, effectuation, closed-competence rejection, audit event `rh.movimentacao.efetivada`, and tenant isolation. The retry fixed the 422 E2E assertion to match the repository error envelope; the final manual recovery hardened `db-bootstrap-smoke.mjs` so HR-03 and FOL-06 select structurally complete employees and FOL-03 smoke-created employees carry `employment_link_id` and `contract_type_id`, clearing the `99-hr03-vacation.sql` failure. Verified with `npm run lint`, `npm run typecheck`, `npm run test`, `DATABASE_URL=postgresql://aarusso@localhost:5432/pecam npm run test:e2e`, `DATABASE_URL=postgresql://aarusso@localhost:5432/pecam npm run db:smoke`, direct `pg_policies` inspection for `hr.employee_transfer`, and `node --check scripts/db-bootstrap-smoke.mjs`.
