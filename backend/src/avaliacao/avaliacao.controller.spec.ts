import { AvaliacaoController } from './avaliacao.controller';

describe('AvaliacaoController', () => {
  it('is defined', () => {
    const controller = new AvaliacaoController({} as never, {} as never);
    expect(controller).toBeDefined();
  });
});
