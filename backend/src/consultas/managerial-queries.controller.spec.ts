import { ManagerialQueriesController } from './managerial-queries.controller';

describe('ManagerialQueriesController', () => {
  it('is defined', () => {
    const controller = new ManagerialQueriesController({} as never);
    expect(controller).toBeDefined();
  });
});
