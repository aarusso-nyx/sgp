# SGP Handoff (2026-04-19)

This handoff captures implementation state for backend/database work completed in this session, with enough detail for another agent to resume safely.

## Scope Completed

- Backend document workflow implemented with S3 pre-signed URLs and PostgreSQL persistence.
- Permission model aligned and tightened for documents.
- Route alignment completed (`register` via path parameter).
- Error semantics aligned for upload registration.
- RLS policy alignment completed for document permissions.
- Seed/bootstrap and governance artifacts updated.
- Build, unit tests, and e2e tests passing.

## Implemented API Behavior

### Documents

- `POST /documents/presign-upload`
  - Requires: `documents:upload`
  - Creates upload session + returns presigned upload URL.

- `POST /documents/uploads/:id/register`
  - Requires: `documents:register`
  - Validates upload session state + S3 object existence.
  - Registers/updates `document_attachment`.
  - Marks upload session `REGISTERED`.

- `GET /documents/:id/presign-download`
  - Requires: `documents:download`
  - Returns short-lived presigned download URL.
  - Writes `document_download_audit`.

- `GET /documents`
  - Requires: `documents:download`

- `GET /documents/:id/download-audit`
  - Requires: `auditoria:read`

### IAM

- `GET /iam/permissions`
  - Requires: `iam:read`.

## Permission Changes

Added runtime permissions:

- `documents:upload`
- `documents:register`
- `documents:download`

Updated group mappings:

- `SGP_RELATORIO` and `RELATORIO` now include all three `documents:*` permissions.
- `SGP_ADMIN` / `ADMIN` include them via full permission set.

## Error Semantics (Register)

- Missing upload session: `404`.
- Invalid/expired session state: `409`.
- Missing S3 object at expected key: `404`.
- S3 infrastructure/validation failure: `503`.

## Database / RLS Alignment

RLS policy updates in `source/database/sql/12-rls-policies.sql`:

- `document_upload_session`:
  - insert => `documents:upload`
  - update => `documents:register`
  - select => `documents:upload` or `documents:register`
- `document_attachment`:
  - select => `documents:download` or `auditoria:read`
  - write => `documents:register`
- `document_download_audit`:
  - insert/select => `documents:download` (plus audit readers for select)

## Seed / Config

- Seed transaction now sets:
  - `set_config('app.bypass_rls', 'true', true)`
- Environment validation now includes S3-related config:
  - `S3_REGION`
  - `S3_DOCUMENTS_BUCKET`
  - `S3_DOCUMENTS_PRESIGN_EXPIRES_SECONDS`
  - `S3_DOCUMENTS_DOWNLOAD_EXPIRES_SECONDS`
  - `S3_DOCUMENTS_KEY_PREFIX`

## Key Files Modified

- Backend:
  - `source/backend/src/documents/documents.controller.ts`
  - `source/backend/src/documents/documents.service.ts`
  - `source/backend/src/documents/documents.dto.ts`
  - `source/backend/src/documents/documents-storage.service.ts`
  - `source/backend/src/documents/documents.module.ts`
  - `source/backend/src/iam/permissions/permission-catalog.ts`
  - `source/backend/src/iam/permissions/permissions.controller.ts`
  - `source/backend/src/iam/iam.module.ts`
  - `source/backend/src/config/environment.ts`
  - `source/backend/prisma/seed.mjs`
  - `source/backend/.env.example`
  - `source/backend/package.json`

- Database:
  - `source/database/sql/11-rls-context.sql`
  - `source/database/sql/12-rls-policies.sql`
  - `source/database/seed/permission-catalog.json`
  - `source/database/legacy-assumptions.md`
  - `source/database/README.md`

- Tests:
  - `source/backend/src/documents/documents.controller.spec.ts`
  - `source/backend/src/documents/documents.service.spec.ts`
  - `source/backend/test/app.e2e-spec.ts`

- Docs/Governance:
  - `source/docs/user/environment.md`
  - `source/docs/eng/99-implementation-status.md`
  - `GOVERNANCE.md`
  - `docs/gov/**`

## Validation Results (Latest)

Executed in `source/backend`:

- `npm run build` => pass
- `npm test` => pass
- `npm run test:e2e` => pass

Known unrelated issue:

- `npm run lint` reports pre-existing errors in `source/backend/src/audit/*` (not introduced by this slice).

## Resume Checklist For Next Agent

1. Install backend dependencies:
   - `cd source/backend && npm install`
2. Ensure env vars are set (especially DB + S3 + Cognito).
3. Apply DB in order:
   - Prisma migrations
   - SQL support files in lexical order under `source/database/sql`
   - seed (`npm run db:seed`)
4. Validate:
   - `npm run build`
   - `npm test`
   - `npm run test:e2e`
5. Continue next backlog item (suggested):
   - tighten domain-level document ownership checks (currently permission + RLS, no ownership rule by owner type).

## Open Decisions / Follow-ups

- Whether to split document permissions further by domain (`rh.documents:*`, `folha.documents:*`).
- Whether to enforce non-null `document_download_audit.user_id` after robust Cognito-sub to user mapping is implemented.
- Whether to add async malware scanning/quarantine in upload registration pipeline.
