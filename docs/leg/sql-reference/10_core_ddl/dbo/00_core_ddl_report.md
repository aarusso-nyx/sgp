# Worker A Core DDL Report

## Scope

- Owner: Worker A
- Writable scope used:
  - `sql/01_prelude/01_create_schema.sql`
  - `sql/10_core_ddl/dbo/10_tables_01.sql`
  - `sql/10_core_ddl/dbo/11_tables_02.sql`
  - `sql/10_core_ddl/dbo/12_tables_03.sql`
  - `sql/10_core_ddl/dbo/30_foreign_keys_01.sql`
  - `sql/10_core_ddl/dbo/31_foreign_keys_02.sql`
  - `sql/10_core_ddl/dbo/32_foreign_keys_03.sql`
- Explicitly untouched due ownership boundaries:
  - `sql/10_core_ddl/dbo/40_indexes.sql`
  - `sql/10_core_ddl/dbo/41_indexes_report.md`

## Objects Found

- Schema in scope: `dbo`
- User tables: `151`
- Columns: `1,745`
- Identity columns: `126`
- Primary keys: `143`
- Unique constraints: `1`
- Foreign keys: `255`
- Check constraints: `0`
- Standalone sequences: `0`
- Computed columns: `0`
- Rowversion columns: `0`

## Objects Converted

- Created `dbo` schema bootstrap SQL in prelude.
- Converted all `151` `dbo` tables into PostgreSQL `CREATE TABLE` statements.
- Converted all in-scope columns using frozen canonical mappings, including:
  - all character types as `TEXT`
  - all timestamp-like types as `TIMESTAMP WITH TIME ZONE`
  - `bit` as `BOOLEAN`
  - `float` as `DOUBLE PRECISION`
  - `numeric` precision/scale preserved
  - `varbinary` as `BYTEA`
- Converted all `126` SQL Server identity columns to `GENERATED BY DEFAULT AS IDENTITY`.
- Converted `7` explicit SQL Server defaults inline on their PostgreSQL columns.
- Converted all `143` primary keys inline on tables.
- Converted the single unique constraint on `dbo.sysdiagrams` inline on the table.
- Converted all `255` foreign keys into ordered post-table `ALTER TABLE ... ADD CONSTRAINT` scripts.
- Included the `dbo.sysdiagrams` backing table for data fidelity, per frozen rules.

## Validation

- Loaded Worker A output into local PostgreSQL scratch database `codex_worker_a_check_20260421`.
- Observed resulting object counts:
  - tables: `151`
  - foreign keys: `255`
  - primary keys: `143`
  - unique constraints: `1`
  - identity columns: `126`
- No PostgreSQL syntax failures remained after shortening two overlong FK identifiers to stay under the PostgreSQL identifier limit.

## Unresolved Items

- None in Worker A scope.

## Assumptions

- The frozen inventory is authoritative even though the live source inventory was taken from `rhlinkcon` while the original request named `rhlinkcom`.
- The `8` source tables without primary keys were preserved without invented keys:
  - `avaliacao_cargo`
  - `avaliacao_funcao`
  - `avaliacao_lotacao`
  - `categoria_profissional_verba`
  - `dia_util`
  - `faixa_salarial_nivel`
  - `historico_contabil`
  - `vinculo_verba`
- Because the frozen inventory shows `0` check constraints, no PostgreSQL `CHECK` clauses were emitted.
- Because the frozen inventory shows `0` standalone sequences, no separate sequence DDL was emitted.
- `sysdiagrams` routines were intentionally not implemented here; only the backing table is included, per canonical rules and Worker A scope.

## Blockers

- None.
