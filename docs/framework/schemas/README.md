# DEVAI Schemas

Executable schemas are supplied by the pinned `@devai-nyx/schemas` registry
package through `@devai-nyx/cli`. This path is the adopter reference surface;
schema files are not copied or hand-maintained in SGP.
