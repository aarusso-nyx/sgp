-- PostgreSQL trigger translation for schema dbo.
-- Inventory found zero source triggers in dbo, so this file is an explicit no-op.

DO $$
BEGIN
    RAISE NOTICE 'No PostgreSQL triggers were created for schema dbo because the SQL Server inventory found zero triggers.';
END
$$;
